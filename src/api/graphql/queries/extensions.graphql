query GetExtensionRepos {
  settings {
    extensionRepos
  }
}

query GetExtensions {
  extensions(first: 2000) {
    totalCount
    nodes {
      pkgName
      name
      lang
      isNsfw
      isInstalled
      iconUrl
      versionName
      hasUpdate
      isObsolete
    }
  }
}
