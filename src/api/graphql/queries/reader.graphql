query GetChapter($id: Int!) {
  chapter(id: $id) {
    id
    name
    chapterNumber
    isRead
    lastPageRead
    pageCount
    sourceOrder
    mangaId
    manga {
      title
      chapters {
        edges {
          node {
            id
            chapterNumber
          }
        }
      }
    }
  }
}
