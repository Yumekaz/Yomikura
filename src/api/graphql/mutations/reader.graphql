mutation FetchChapterPages($input: FetchChapterPagesInput!) {
  fetchChapterPages(input: $input) {
    pages
  }
}

mutation UpdateChapterProgress($input: UpdateChapterInput!) {
  updateChapter(input: $input) {
    chapter {
      id
      isRead
      lastPageRead
    }
  }
}
